# Crawler output — huge, and the whole point is it never lives in the repo
archive/
wget.log
*.log

# Site-specific adapters (named site_<label>.py, e.g. site_a.py). These can
# encode enough of a source site's real markup/structure to fingerprint it,
# so they stay local, never committed. The reusable framework (base.py,
# example_adapter.py, scripts/parse_site.py) is safe to commit.
src/storyindex/adapters/site_*.py

# Story signatures / tag candidates drop folders (generated, not source)
drop/
signatures/

# Local databases
*.db
*.sqlite
*.sqlite3

# Working library directory - a real, in-use SQLite library plus whatever
# else you keep alongside it locally. Doubly covered by *.sqlite above,
# but named explicitly since this is the intended place to put one.
library/

# scripts/backup.sh's output - real library snapshots. Doubly covered by
# *.sqlite above, named explicitly for the same reason as library/.
backups/

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node (if the local web app UI ends up JS-based)
node_modules/
dist/
build/

# Env / secrets
.env
.env.*

# Scratch space for pulling out specific content (story text, tag
# examples, prompt output) into a file for review — e.g. sharing a
# handful of stories/tags with a client or checking tagging quality by
# eye. Never committed: whatever ends up here can include real library
# content and shouldn't be assumed safe to publish.
review/

# Editor / OS cruft
.vscode/
.idea/
.DS_Store
*.swp
